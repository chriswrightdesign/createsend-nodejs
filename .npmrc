# This package installs from and publishes to the public npm registry,
# overriding any parent/global default (e.g. the Nexus registry in ~/.npmrc).
registry=https://registry.npmjs.org/
@create-send:registry=https://registry.npmjs.org/
